server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/datee?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: 9836
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  root: info
